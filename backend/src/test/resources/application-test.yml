spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/skillbarter_test
    username: skillbarter_user
    password: skillbarter_pass
    
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  redis:
    host: localhost
    port: 6379

logging:
  level:
    com.skillbarter: WARN
